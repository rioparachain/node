# Cargo compiled files and executables
**/target/

# These are backup files generated by rustfmt
**/*.rs.bk

# Local chain databases (defalut location)
**/chains/

# The cache for chain data in container
.local

# The cache for docker container dependency
/.cargo/config

# Submodules dir
submodules

# Some tmp files
**/*.swp
tmp

docker/.chainspec
**/node_modules

# Folders with patches

cumulus/client
cumulus/primitives

node/relaychain-rio-cli/**/*.tmp
node/relaychain-rio-cli/**/*.toml
node/relaychain-rio-cli/**/*.rs

node/relaychain-rio-service/**/*.tmp
node/relaychain-rio-service/**/*.toml
node/relaychain-rio-service/**/*.rs

node/relaychain-rio-client/**/*.tmp
node/relaychain-rio-client/**/*.toml
node/relaychain-rio-client/**/*.rs

runtime/relaychain-rio/**/*.tmp
runtime/relaychain-rio/**/*.toml
runtime/relaychain-rio/**/*.rs

pallets/transaction-payment/**/*.tmp
pallets/transaction-payment/**/*.toml
pallets/transaction-payment/**/*.rs

pallets/assets/**/*.tmp
pallets/assets/**/*.toml
pallets/assets/**/*.rs

.DS_Store
.idea
.vscode

